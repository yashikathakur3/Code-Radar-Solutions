#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    char a;
    scanf("%c",&a);
    int c=a;
    if((a>=65)&&(a<=90)){
        printf("ASCII Value %d":a);
    }
    if((a>=97)&&(a<=122)){
        printf("ASCII Value %d":a);
    }


    return 0;
}